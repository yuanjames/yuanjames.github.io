<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://yuanjames.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yuanjames.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2024-07-20T09:31:31+00:00</updated><id>https://yuanjames.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">GPUs Server Notes</title><link href="https://yuanjames.github.io/blog/2022/GPU-Server-Notes/" rel="alternate" type="text/html" title="GPUs Server Notes"/><published>2022-06-09T00:00:00+00:00</published><updated>2022-06-09T00:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2022/GPU-Server-Notes</id><content type="html" xml:base="https://yuanjames.github.io/blog/2022/GPU-Server-Notes/"><![CDATA[<p><strong>UPDATE 2022</strong></p> <p>Due to the university security updates, the ways of accessing our GPU server and uploading/updating your code are changed, and one new solution is shown step by step below.</p> <ol> <li>Email <code class="language-plaintext highlighter-rouge">ishelp@hw.ac.uk</code> for granting access, quoting your school and the machines you need to log in to.</li> <li>Once you are granted, you will be provided with a secret key to configure the authenticator app (e.g., Microsoft Authenticator or Google Authenticator).</li> <li>Now, you should be able to connect to the university gateway by <code class="language-plaintext highlighter-rouge">ssh -p 44788 usr @sshgw.hw.ac.uk</code>. You will be asked password and verification code, the latter comes from the authenticator app. (p.s, sshgw looks like a security door for anyone who wants to visit university server from external network)</li> <li>If you already succeeded to log in <code class="language-plaintext highlighter-rouge">sshgw</code>, now you can continue to do <code class="language-plaintext highlighter-rouge">ssh usr@jove.macs.hw.ac.uk</code>, and <code class="language-plaintext highlighter-rouge">ssh thanos</code>. Now, you arrive the destination.</li> <li>The problem we are facing is <code class="language-plaintext highlighter-rouge">sshgw</code> may restrict some applications. For example, in VScode, the plugin Remote Explore is disabled, the connection is always suspended after the establishing. Therefore, we need the <code class="language-plaintext highlighter-rouge">port forwarding</code>.</li> <li>In my case, I used FileZilla to upload code and download experimental results. FileZilla is the same as most of the applications, not allowed to directly connect to <code class="language-plaintext highlighter-rouge">jove</code> because we need to pass the security door <code class="language-plaintext highlighter-rouge">sshgw</code> at first. Obviously, our connection has twice jumps, <code class="language-plaintext highlighter-rouge">from local to sshw</code> and <code class="language-plaintext highlighter-rouge">from sshw to jove</code>. Therefore, <code class="language-plaintext highlighter-rouge">sshw</code> seems like a proxy.</li> <li>Download PuTTy. In <code class="language-plaintext highlighter-rouge">Session</code>, type in host name <code class="language-plaintext highlighter-rouge">sshgw.hw.ac.uk</code> with port 44788. In <code class="language-plaintext highlighter-rouge">Connection-SSH-Tunnels</code>, select <code class="language-plaintext highlighter-rouge">Dynamic</code>, and type in any port number you want to use in <code class="language-plaintext highlighter-rouge">Source port</code>, click <code class="language-plaintext highlighter-rouge">Add</code>, for example, 5000. Then, click Open, you will need to type in password and verification code. If the above steps done, you open a tunnel from you localhost port 5000 to university <code class="language-plaintext highlighter-rouge">sshgw</code>, i.e, your any requests sent via the port 5000 will be directed to the sshgw to the destination.</li> <li>Download FileZilla. Open <code class="language-plaintext highlighter-rouge">site manager</code>, you can add a host <code class="language-plaintext highlighter-rouge">jove.macs.hw.ac.uk</code> with selecting <code class="language-plaintext highlighter-rouge">SFTP protocal</code> and denoting <code class="language-plaintext highlighter-rouge">User</code> with your username.</li> <li>Go to the <code class="language-plaintext highlighter-rouge">Edit-Settings-Generic Proxy</code>, select <code class="language-plaintext highlighter-rouge">SOCKS5</code> and denote <code class="language-plaintext highlighter-rouge">Proxy host</code> with localhost and <code class="language-plaintext highlighter-rouge">Proxy port</code> with 5000.</li> <li>Now, you should be able to access GPU file system via FileZilla.</li> </ol> <p><strong>———————————————-Deprecated———————————————–</strong></p> <ol> <li> <p>Contact HW MACS admin to get the grant for accessing GPUs.</p> </li> <li> <p>The command <code class="language-plaintext highlighter-rouge">ssh xxx@ssh.macs.hw.ac.uk</code> with the password to access into MACS network. You can use <a href="https://en.wikipedia.org/wiki/Secure_Shell_Protocol">ssh</a> in MacOS/Linux terminal or tools (e.g., PuTTY, X2Go) in Windows.</p> </li> <li> <p>The command <code class="language-plaintext highlighter-rouge">ssh thanos</code> to connect GPUs resources. You may find the files and directory by <code class="language-plaintext highlighter-rouge">ls</code>, or <code class="language-plaintext highlighter-rouge">ls -l</code> for more details. <code class="language-plaintext highlighter-rouge">thanos</code> and <code class="language-plaintext highlighter-rouge">ssh.macs.hw.ac.uk</code> share the same home directonry.</p> </li> <li> <p>You may find the conda from <code class="language-plaintext highlighter-rouge">/opt/anaconda3/bin</code>. Conda is an open-source, cross-platform, language-agnostic package manager and environment management system.</p> </li> <li> <p>Add the path <code class="language-plaintext highlighter-rouge">'export PATH="/opt/anaconda3/bin:$PATH"'</code> into <code class="language-plaintext highlighter-rouge">~/.bashrc</code> . Then, you can use the command <code class="language-plaintext highlighter-rouge">conda</code> in your home directory.</p> </li> <li> <p>The <code class="language-plaintext highlighter-rouge">bashrc</code> file is not executed (for no login shell), so you need to <code class="language-plaintext highlighter-rouge">vim ~/.bash_profile</code> and add the below code. Otherwise, you need to do <code class="language-plaintext highlighter-rouge">source ~/.bashrc</code> every time when you log in by <code class="language-plaintext highlighter-rouge">ssh</code>.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> if [ -f ~/.bashrc ] ; then

 source .bashrc

 fi
</code></pre></div> </div> </li> <li> <p>Execute the command <code class="language-plaintext highlighter-rouge">source ~/.bashrc</code> to take effect.</p> </li> <li> <p>The command <code class="language-plaintext highlighter-rouge">conda create -n myenv python=3.*</code> is used to create your python environment.</p> </li> <li> <p>After you create the myenv, you can use the command <code class="language-plaintext highlighter-rouge">source activate myenv</code> (old conda version) to activate and get in the environment.</p> </li> <li> <p><code class="language-plaintext highlighter-rouge">conda ***</code> install your required packages/libs.</p> </li> <li> <p>The command <code class="language-plaintext highlighter-rouge">nvidia-smi</code> can be used to check GPU peformance and information, <code class="language-plaintext highlighter-rouge">nvidia-smi -l 1</code> to update the GPU (dynamic) information per second. <code class="language-plaintext highlighter-rouge">htop</code> can be used to check CPU information.</p> </li> <li> <p>If you suspend your programme manually, remember to release GPU memory by <code class="language-plaintext highlighter-rouge">kill -9 PID</code>.</p> </li> <li> <p>If you do not want you programme suspended when you log out, please use <code class="language-plaintext highlighter-rouge">nohup</code>. For example, <code class="language-plaintext highlighter-rouge">nohup python xxx.py &gt; output.txt 2&gt;&amp;1 &amp;</code>. 2&gt;1&amp; means the stderr will be directed to stdout. see details <a href="https://en.wikipedia.org/wiki/Nohup">nohup</a></p> </li> <li> <p>In order to upload your code to server, you can run <code class="language-plaintext highlighter-rouge">git clone</code> (if you have already build the repositoary in Github, Gitlab etc). Also, you can use <code class="language-plaintext highlighter-rouge">git pull</code> to update your code in server. Do remember, your code may have some outputs (e.g., csv), they are not tracked by git until the commit, which means the outputs will not be synchronized (deleted) by <code class="language-plaintext highlighter-rouge">git pull</code>. In case the ouputs affects next round experimental output, you can <code class="language-plaintext highlighter-rouge">rm xxx</code> or <code class="language-plaintext highlighter-rouge">git clean</code> them.</p> </li> <li> <p>If you have some outputs (e.g. txt, csv), you can use <code class="language-plaintext highlighter-rouge">scp xxx@ssh.macs.hw.ac.uk:~/xxx/xxx/targetfile ~/localfile</code> to copy the file from server to local. see details <a href="https://en.wikipedia.org/wiki/Secure_copy_protocol#:~:text=Secure%20copy%20protocol%20(SCP)%20is,Protocol%20and%20the%20program%20itself.">scp</a></p> </li> <li> <p>If you are not familiar with Unix commands, you can use <a href="https://code.visualstudio.com/docs/remote/ssh">Remote Development using SSH</a>. You just need to open Vscode and install the extension. Then, you will able to access the server directory with user interface after you config the <code class="language-plaintext highlighter-rouge">ssh</code> connection. You may not able to directly connect to ‘thanos’, so you need to config the ssh with the jump from jove to thanos (see below). Then you can set the conda environment in the server as your local environment. (P.S. You need to download Openssh via Windows10’s ‘manage optional feature’ as admin if you use Windows)</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host jove
HostName ssh.macs.hw.ac.uk
User xxxx

Host thanos
HostName thanos.macs.hw.ac.uk
User xxx
ProxyCommand C:\Windows\System32\OpenSSH\ssh.exe -W %h:%p jove ## (windows)
ProxyCommand ssh -W %h:%p jove ## (Ubuntu)
</code></pre></div> </div> </li> <li> <p>Specify a GPU to run your programme by <code class="language-plaintext highlighter-rouge">os.environ['CUDA_VISIBLE_DEVICES'] = '2'</code></p> </li> <li> <p>For convenience, (In Linux) you can <code class="language-plaintext highlighter-rouge">vim ~/.ssh/config</code> and add the below code. Then, you can use <code class="language-plaintext highlighter-rouge">ssh hw</code> to log in, instead of <code class="language-plaintext highlighter-rouge">ssh xxx@ssh.macs.hw.ac.uk</code>. This revision also works for <code class="language-plaintext highlighter-rouge">scp</code>.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host hw

Hostname ssh.macs.hw.ac.uk

User xxx
</code></pre></div> </div> </li> <li> <p>You may favour <a href="https://en.wikipedia.org/wiki/Tmux">tmux terminal</a>, it provides more features than ubuntu terminal.</p> </li> <li> <p>When you run your code in GPUs, strongly suggest you set appropriate batch size (128, 256, etc), larger batch size will help GPUs release more power.</p> </li> <li> <p>GPU memory and utility are affected each other to some degree. Memory consuming depends on the size and complexity of model, and batch size. Large batch size will increase the GPU utility. However, the utility really also depends on model complexity. If a model simple enough, whatever how large of your batch size, the utility always keeps a low level.</p> </li> <li> <p>Using Pytorch no_grad() when you evaluate or test model would help you to improve GPUs utilities and save GPUs memory. You can also manually release GPU memory and reflect on <code class="language-plaintext highlighter-rouge">Nvidia-smi</code> by <code class="language-plaintext highlighter-rouge">torch.cuda.empty_cache()</code>. Even Pytorch will release GPU memory automatically like Python memory management, but it may not reflect on nividia-smi immediately.</p> </li> <li> <p>By default, TensorFlow maps nearly all of the GPU memory of all GPUs (subject to CUDA_VISIBLE_DEVICES) visible to the process. This is done to more efficiently use the relatively precious GPU memory resources on the devices by reducing memory fragmentation. To limit TensorFlow to a specific set of GPUs we use the <code class="language-plaintext highlighter-rouge">tf.config.experimental.set_visible_devices</code> method. <a href="https://www.tensorflow.org/guide/gpu#:~:text=Limiting%20GPU%20memory%20growth,-By%20default%2C%20TensorFlow&amp;text=To%20limit%20TensorFlow%20to%20a,config.experimental.set_visible_devices%20method.&amp;text=In%20some%20cases%20it%20is,is%20needed%20by%20the%20process.">Details</a></p> </li> </ol>]]></content><author><name></name></author><category term="knowlegde-note"/><summary type="html"><![CDATA[Guidelines for SMML HWU GPUs]]></summary></entry><entry><title type="html">Review of Error Backpropagation</title><link href="https://yuanjames.github.io/blog/2021/BP/" rel="alternate" type="text/html" title="Review of Error Backpropagation"/><published>2021-12-08T12:00:00+00:00</published><updated>2021-12-08T12:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2021/BP</id><content type="html" xml:base="https://yuanjames.github.io/blog/2021/BP/"><![CDATA[<ol> <li> <p>Output layer error:</p> \[E=\frac{1}{2}(\hat{y}-y)^{2}=\frac{1}{2}\left(g_{o}\left(a_{1}^{m}\right)-y\right)^{2}\] </li> <li> <p>Derivative for weight:</p> \[\frac{\partial E}{\partial w_{i j}^{k}}=\frac{\partial E}{\partial a_{j}^{k}} \frac{\partial a_{j}^{k}}{\partial w_{i j}^{k}}\] \[\frac{\partial E}{\partial w_{i j}^{k}}=\delta_{j}^{k} o_{i}^{k-1}\] </li> <li> <p>\(\frac{\partial a_{j}^{k}}{\partial w_{i j}^{k}}\):</p> \[\frac{\partial a_{j}^{k}}{\partial w_{i j}^{k}}=\frac{\partial}{\partial w_{i j}^{k}}\left(\sum_{l=0}^{r_{k-1}} w_{l j}^{k} o_{l}^{k-1}\right)=o_{i}^{k-1}\] </li> <li> <p>\(\delta\):</p> \[\delta_{j}^{k}=\frac{\partial E}{\partial a_{j}^{k}}=\sum_{l=1}^{r^{k+1}} \frac{\partial E}{\partial a_{l}^{k+1}} \frac{\partial a_{l}^{k+1}}{\partial a_{j}^{k}}\] \[\frac{\partial a_{l}^{k+1}}{\partial a_{j}^{k}}=w_{j l}^{k+1} g^{\prime}\left(a_{j}^{k}\right)\] </li> <li> <p>Overall:</p> \[\frac{\partial E}{\partial w_{i j}^{k}}=\delta_{j}^{k} o_{i}^{k-1}=g^{\prime}\left(a_{j}^{k}\right) o_{i}^{k-1} \sum_{l=1}^{r^{k+1}} w_{j l}^{k+1} \delta_{l}^{k+1}\] </li> </ol>]]></content><author><name></name></author><category term="note"/><summary type="html"><![CDATA[Output layer error:]]></summary></entry><entry><title type="html">The note for installing a series of requirements for deep learning</title><link href="https://yuanjames.github.io/blog/2021/Laptop-Config/" rel="alternate" type="text/html" title="The note for installing a series of requirements for deep learning"/><published>2021-11-21T12:00:00+00:00</published><updated>2021-11-21T12:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2021/Laptop-Config</id><content type="html" xml:base="https://yuanjames.github.io/blog/2021/Laptop-Config/"><![CDATA[<p>I have experienced many times that installing and configuring new environment (including python, libraries, CUDA etc), this is very ANNOYING. So, I am here to make some notes for convinience in future.</p> <p>Operation system: Ubuntu</p> <p>1.Vscode - Go to the https://code.visualstudio.com/, download the Debian version, use the command <code class="language-plaintext highlighter-rouge">sudo dpkg -i &lt;file&gt;.deb</code> to install it.</p> <p>2.Anaconda - Go to the https://docs.anaconda.com/anaconda/install/linux/ , download the Debian version. The following commands are used to verfiy and install the Anaconda. During the installation, the offical recommend you to accept the default install location.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    sha256sum /path/filename
    bash ~/Downloads/Anaconda3-2020.02-Linux-x86_64.sh

    conda create -n name python=3.xx
    conda activate name
</code></pre></div></div> <p>3.CUDA Toolkit（Nvidia Official, rather than from Pytorch) - ‘CUDA is a software layer that gives direct access to the GPU’s virtual instruction set and parallel computational elements’. When you install CUDA Toolkit, you will get a Nvidia driver: for your GPUs, GPU-accelerated libraries, debugging and optimization tools, a C/C++ compiler, and a runtime library. See below commands for Ubuntu 20.04 and CUDA 11.03. Then, you will find the driver and CUDA are installed by <code class="language-plaintext highlighter-rouge">nvidia-smi</code>.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    wget https://developer.download.nvidia.com/compute/cuda/repos/ubuntu2004/x86_64/cuda-ubuntu2004.pin
    sudo mv cuda-ubuntu2004.pin /etc/apt/preferences.d/cuda-repository-pin-600
    wget https://developer.download.nvidia.com/compute/cuda/11.3.0/local_installers/cuda-repo-ubuntu2004-11-3-local_11.3.0-465.19.01-1_amd64.deb
    sudo dpkg -i cuda-repo-ubuntu2004-11-3-local_11.3.0-465.19.01-1_amd64.deb
    sudo apt-key add /var/cuda-repo-ubuntu2004-11-3-local/7fa2af80.pub
    sudo apt-get update
    sudo apt-get -y install cuda
</code></pre></div></div> <p>From here, you can download Pytorch by <code class="language-plaintext highlighter-rouge">conda install pytorch torchvision torchaudio cudatoolkit=10.2 -c pytorch</code> to run your code over the GPU(s), because the CUDA/cuDNN files required by PyTorch come with it already (p.s. this cudatoolkit is not related to the official one to some degree, or in other words Pytorch works alone only required driver).</p> <p>Additionally, you may find the <code class="language-plaintext highlighter-rouge">nvcc -V</code> gives your error. You could deal with it by adding some libraries paths <code class="language-plaintext highlighter-rouge">export PATH=$PATH:/usr/local/cuda/bin</code> to <code class="language-plaintext highlighter-rouge">.bashrc</code> file.</p> <p>4.cuDNN - The NVIDIA CUDA® Deep Neural Network library. If you need to use Tenrsorflow, it means you have to install cuDNN libraries. Please find the approprite version from https://developer.nvidia.com/rdp/cudnn-archive.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    Download the cuDNN (https://developer.nvidia.com/rdp/cudnn-archive): cuDNN Runtime Library for Ubuntu20.04 x86_64 (Deb), cuDNN Developer Library for Ubuntu20.04 x86_64 (Deb), cuDNN Code Samples and User Guide for Ubuntu20.04 x86_64 (Deb)
    
    sudo dpkg -i libcudnn8_8.2.4.15-1+cuda11.4_amd64.deb 
    sudo dpkg -i libcudnn8-dev_8.2.4.15-1+cuda11.4_amd64.deb
    sudo dpkg -i libcudnn8-samples_8.2.4.15-1+cuda11.4_amd64.deb

    In this way, you do have to configure the path becasue dpkg ubuntu system tool will help to install this in system with the path.
</code></pre></div></div>]]></content><author><name></name></author><category term="instruction"/><summary type="html"><![CDATA[Installation of CUDA, cuDNN, Pytorch, and Tensorflow]]></summary></entry><entry><title type="html">Python Note</title><link href="https://yuanjames.github.io/blog/2021/Python-Note/" rel="alternate" type="text/html" title="Python Note"/><published>2021-06-21T12:00:00+00:00</published><updated>2021-06-21T12:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2021/Python-Note</id><content type="html" xml:base="https://yuanjames.github.io/blog/2021/Python-Note/"><![CDATA[<p>Decorators: A Python decorator is a specific change to the Python syntax that allows us to add more code to your orginal code.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>def my_decorator(func):
    @wrap(func)
    def wrapper():
        print("Something is happening before the function is called.")
        return func()
    print("Something is happening after the function is called.")
    return wrapper

@my_decorator
def say_whee():
    print("Whee!")
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">functools.wraps</code> in fact is equivalent to the combination of <code class="language-plaintext highlighter-rouge">partial()</code> and <code class="language-plaintext highlighter-rouge">update_wrapper()</code>.</p> <p><code class="language-plaintext highlighter-rouge">t=partial(f, x, z=g)</code> is used to generate the new ‘<code class="language-plaintext highlighter-rouge">f</code>’ function <code class="language-plaintext highlighter-rouge">t</code> with the supplied new positional argument <code class="language-plaintext highlighter-rouge">x</code> or keyword argument <code class="language-plaintext highlighter-rouge">z</code>. Meanwhile, new function <code class="language-plaintext highlighter-rouge">t</code> will lose the orginal details of it, but you can call <code class="language-plaintext highlighter-rouge">update_wrapper(t,f)</code> to solve this issue. When you are applying decorator, you still can use ‘@wrap(say_whee)’ to keep the <code class="language-plaintext highlighter-rouge">__doc__</code> and <code class="language-plaintext highlighter-rouge">__name__</code> of func with it, instead of <code class="language-plaintext highlighter-rouge">wrapper</code></p>]]></content><author><name></name></author><category term="knowlegde-note"/><summary type="html"><![CDATA[Python Decorator, functools.wraps, functools.update_wrapper, partial differences.]]></summary></entry><entry><title type="html">Python Tricky</title><link href="https://yuanjames.github.io/blog/2021/Python-Tricky/" rel="alternate" type="text/html" title="Python Tricky"/><published>2021-06-01T12:00:00+00:00</published><updated>2021-06-01T12:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2021/Python-Tricky</id><content type="html" xml:base="https://yuanjames.github.io/blog/2021/Python-Tricky/"><![CDATA[<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>import numpy as np

a = np.array( [[1,3,4,1],[4,5,6,7], [1,3,4,1], [0,0,0,0]])
for p in a:
    a = []
    print(p)
</code></pre></div></div> <p>Output:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1 3 4 1]
[4 5 6 7]
[1 3 4 1]
[0 0 0 0]

</code></pre></div></div> <p>Python has Iterable, Iterator, Generator. Iterable requires to implement the function <code class="language-plaintext highlighter-rouge">__iter__</code>. Iterator needs both <code class="language-plaintext highlighter-rouge">__iter__</code> and <code class="language-plaintext highlighter-rouge">__next__</code>. While Generator is an iterator. Generator contains <code class="language-plaintext highlighter-rouge">yield</code> or <code class="language-plaintext highlighter-rouge">(xx for in xx expression xx)</code>. In Python, for keyword will invoke <code class="language-plaintext highlighter-rouge">iter()</code> function to wrap iteratable objects to be iterators.</p> <p>Immutable data types: Number, String, Tuple. Mutable data types: list, dictionary.</p> <p>Immutable is insensitive to <code class="language-plaintext highlighter-rouge">=</code>. While, <code class="language-plaintext highlighter-rouge">a = b</code> operation for mutable data will share the same object, change a/b will affect another one. Shallow copy: copy(), slice operation, list() and Deepcopy().</p> <p>Super(xx, self).function() is used to call the parent class function while solve the MRO problem. The problem will mess up programme when parentclass().function() is called.</p>]]></content><author><name></name></author><category term="knowlegde-note"/><summary type="html"><![CDATA[``` import numpy as np]]></summary></entry><entry><title type="html">Fourier transform and GNN</title><link href="https://yuanjames.github.io/blog/2021/GNNFT/" rel="alternate" type="text/html" title="Fourier transform and GNN"/><published>2021-05-20T12:00:00+00:00</published><updated>2021-05-20T12:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2021/GNNFT</id><content type="html" xml:base="https://yuanjames.github.io/blog/2021/GNNFT/"><![CDATA[<p>GNN - https://zhuanlan.zhihu.com/p/137897522</p> <p>FT - https://charlesliuyx.github.io/2018/02/18/%E3%80%90%E7%9B%B4%E8%A7%82%E8%AF%A6%E8%A7%A3%E3%80%91%E8%AE%A9%E4%BD%A0%E6%B0%B8%E8%BF%9C%E5%BF%98%E4%B8%8D%E4%BA%86%E7%9A%84%E5%82%85%E9%87%8C%E5%8F%B6%E5%8F%98%E6%8D%A2%E8%A7%A3%E6%9E%90/</p>]]></content><author><name></name></author><category term="knowlegde-note"/><summary type="html"><![CDATA[GNN - https://zhuanlan.zhihu.com/p/137897522]]></summary></entry><entry><title type="html">Erratum</title><link href="https://yuanjames.github.io/blog/2021/Erratum/" rel="alternate" type="text/html" title="Erratum"/><published>2021-04-27T12:00:00+00:00</published><updated>2021-04-27T12:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2021/Erratum</id><content type="html" xml:base="https://yuanjames.github.io/blog/2021/Erratum/"><![CDATA[<p>Erratum for A Systematic Comparison Study on Hyperparameter Optimisation of Graph Neural Networks for Molecular Property Prediction</p> <p>There are two typos in the paper: A Systematic Comparison Study on Hyperparameter Optimisation of Graph Neural Networks for Molecular Property Prediction. The paper is published in GECCO 2021.</p> <p>In Algorithm 1, U means uniform distribution rather than normal distribution. It is a typo error; in the paper, Section 2.1 gives the explanation for uniform distribution.</p> <p>In the Section 4.1 Experimental Settings, the significance level should be 10% rather than 5% (i.e, 5% is for a side, so the significance level should 10% when two-tailed test is applied). However, this error does not affect experimental results.</p>]]></content><author><name></name></author><category term="knowlegde-note"/><summary type="html"><![CDATA[Erratum for A Systematic Comparison Study on Hyperparameter Optimisation of Graph Neural Networks for Molecular Property Prediction]]></summary></entry><entry><title type="html">MolCLR</title><link href="https://yuanjames.github.io/blog/2021/MolCLR/" rel="alternate" type="text/html" title="MolCLR"/><published>2021-03-01T12:00:00+00:00</published><updated>2021-03-01T12:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2021/MolCLR</id><content type="html" xml:base="https://yuanjames.github.io/blog/2021/MolCLR/"><![CDATA[<p>Graph feature learning via graph neural networks. There are three main ideas: atom masking, bond deletion, and subgraph removal to implement data augmentation. A GNN is then used to encode the molecule graph. Meanwhile, a contrastive estimator is utilized to maximize the agreement of different graph augmentations from the same molecule.</p> <p>source: https://arxiv.org/pdf/2102.10056.pdf</p>]]></content><author><name></name></author><category term="knowlegde-note"/><summary type="html"><![CDATA[Graph feature learning via graph neural networks. There are three main ideas: atom masking, bond deletion, and subgraph removal to implement data augmentation. A GNN is then used to encode the molecule graph. Meanwhile, a contrastive estimator is utilized to maximize the agreement of different graph augmentations from the same molecule.]]></summary></entry><entry><title type="html">CMA-ES and TPE</title><link href="https://yuanjames.github.io/blog/2021/CMA-ES-TPE/" rel="alternate" type="text/html" title="CMA-ES and TPE"/><published>2021-02-06T12:00:00+00:00</published><updated>2021-02-06T12:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2021/CMA-ES-TPE</id><content type="html" xml:base="https://yuanjames.github.io/blog/2021/CMA-ES-TPE/"><![CDATA[<p>CMA-ES and TPE are two advanced optimisation methods currently which have been used to solve HPO problems.</p> <p>The main idea of CMA-ES is multivariate distribution, with evolution strategy. After each generation evaluated, the distribution will be updated by their novel ideas.</p> <p>In contrast, TPE makes use of surrogate model to approximate the real situation, meanwhile, draws candidates and select the most promosing one to be evaluated.</p>]]></content><author><name></name></author><category term="knowlegde-note"/><summary type="html"><![CDATA[CMA-ES and TPE are two advanced optimisation methods currently which have been used to solve HPO problems.]]></summary></entry><entry><title type="html">Optuna User-defined Sampler</title><link href="https://yuanjames.github.io/blog/2021/Optuna/" rel="alternate" type="text/html" title="Optuna User-defined Sampler"/><published>2021-01-19T12:00:00+00:00</published><updated>2021-01-19T12:00:00+00:00</updated><id>https://yuanjames.github.io/blog/2021/Optuna</id><content type="html" xml:base="https://yuanjames.github.io/blog/2021/Optuna/"><![CDATA[<p>Optuna provodes a complete framwork for Hyperparameter Optimisation.</p> <p>The basic of Optuna process starts from creating a study, sampler and pruner can be passed into the study. The study.opitimise() is used to optimise an objective function in which neural network models are implmeneted, configured with HPS, trained, and return validation scores.</p> <p>Optuna contains several key components: Pruner, Sampler, Trial, Study.</p> <p>The Smapler is used to define hyperparameters search method/strategy. The sampler containd three important things: infer_relative_search_space, sample_relative, and sample_independant. infer_relative_search_space is used to judge which hyparameters and their value ranges (search_space) will be passed into sample_relative method. sample_relative essentially is implemented to sample some relative values in the form of Dict class. Then trial.suggest method is called to receive the values in term of API by checking the Dict key. Finally, sample_independant is the most simple one, which is totally random algorithms for different types HPS (e.g., int, float), by passing the param_name: str, and param_distribution: distributions.BaseDistribution. But, the sample_independant are excueted automatically for those HPS not appear in search_space.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"""
Optuna example that implements a user-defined relative sampler based on Simulated Annealing
algorithm. Please refer to https://en.wikipedia.org/wiki/Simulated_annealing for Simulated
Annealing itself.
Note that this implementation isn't intended to be used for production purposes and
has the following limitations:
- The sampler only supports `UniformDistribution` (i.e., `Trial.suggest_uniform` method).
- The implementation prioritizes simplicity over optimization efficiency.
You can run this example as follows:
    $ python simulated_annealing_sampler.py
"""

import numpy as np

import optuna
from optuna import distributions
from optuna.samplers import BaseSampler
from optuna.study import StudyDirection
from optuna.trial import TrialState


class SimulatedAnnealingSampler(BaseSampler):
    def __init__(
        self, temperature=100, cooldown_factor=0.9, neighbor_range_factor=0.1, seed=None
    ):
        self._rng = np.random.RandomState(seed)
        self._independent_sampler = optuna.samplers.RandomSampler(seed=seed)
        self._temperature = temperature
        self.cooldown_factor = cooldown_factor
        self.neighbor_range_factor = neighbor_range_factor
        self._current_trial = None
        self.i = 5
    def infer_relative_search_space(self, study, trial):
        print(
            "\ninfer_relative_search_space",
            optuna.samplers.intersection_search_space(study),
        )
        return optuna.samplers.intersection_search_space(study)

    def sample_relative(self, study, trial, search_space):
        if search_space == {}:
            # The relative search space is empty (it means this is the first trial of a study).
            return {}

        # The rest of this method is an implementation of Simulated Annealing (SA) algorithm.
        prev_trial = self._get_last_complete_trial(study)

        # Update the current state of SA if the transition is accepted.
        if self._rng.uniform(0, 1) &lt;= self._transition_probability(study, prev_trial):
            self._current_trial = prev_trial

        # Pick a new neighbor (i.e., parameters).
        params = self._sample_neighbor_params(search_space)

        # Decrease the temperature.
        self._temperature *= self.cooldown_factor

        print("sample_relative", params)
        return params

    def _sample_neighbor_params(self, search_space):
        # Generate a sufficiently near neighbor (i.e., parameters).
        #
        # In this example, we define a sufficiently near neighbor as
        # `self.neighbor_range_factor * 100` percent region of the entire
        # search space centered on the current point.

        params = {}
        for param_name, param_distribution in search_space.items():
            if isinstance(param_distribution, distributions.UniformDistribution):
                current_value = self._current_trial.params[param_name]
                width = (
                    param_distribution.high - param_distribution.low
                ) * self.neighbor_range_factor
                neighbor_low = max(current_value - width, param_distribution.low)
                neighbor_high = min(current_value + width, param_distribution.high)
                params[param_name] = self._rng.uniform(neighbor_low, neighbor_high)

                print(
                    "param_name",
                    param_name,
                    "neighbor_low",
                    neighbor_low,
                    "neighbor_high",
                    neighbor_high,
                )
            else:
                raise NotImplementedError(
                    "Unsupported distribution {}.".format(param_distribution)
                )

        return params

    def _transition_probability(self, study, prev_trial):
        if self._current_trial is None:
            return 1.0

        prev_value = prev_trial.value
        current_value = self._current_trial.value

        # `prev_trial` is always accepted if it has a better value than the current trial.
        if study.direction == StudyDirection.MINIMIZE and prev_value &lt;= current_value:
            return 1.0
        elif study.direction == StudyDirection.MAXIMIZE and prev_value &gt;= current_value:
            return 1.0

        # Calculate the probability of accepting `prev_trial` that has a worse value than
        # the current trial.
        return np.exp(-abs(current_value - prev_value) / self._temperature)

    @staticmethod
    def _get_last_complete_trial(study):
        complete_trials = [t for t in study.trials if t.state == TrialState.COMPLETE]
        return complete_trials[-1]

    def sample_independent(self, study, trial, param_name, param_distribution):
        # In this example, this method is invoked only in the first trial of a study.
        # The parameters of the trial are sampled by using `RandomSampler` as follows.
        return self._independent_sampler.sample_independent(
            study, trial, param_name, param_distribution
        )


# Define a simple 2-dimensional objective function whose minimum value is -1 when (x, y) = (0, -1).
def objective(trial):
    print(sampler.i)
    sampler.i+=1
    if sampler.i &gt;7:
        x = trial.suggest_float("x", -100, 100)
        y = trial.suggest_float("y", -1, 1)
        # z = trial.suggest_float("z", -50, 50)
        return x ** 2 + y 
    x = trial.suggest_float("x", -100, 100)
    y = trial.suggest_float("y", -1, 1)
    z = trial.suggest_float("z", -50, 50)
    return x ** 2 + y +z


if __name__ == "__main__":
    # Run optimization by using `SimulatedAnnealingSampler`.
  
    sampler = SimulatedAnnealingSampler()
    study = optuna.create_study(sampler=sampler)
    study.optimize(objective, n_trials=5)
    
    print("Best trial:")
    print("  Value: ", study.best_trial.value)
    print("  Params: ")
    for key, value in study.best_trial.params.items():
        print("    {}: {}".format(key, value))


</code></pre></div></div>]]></content><author><name></name></author><category term="knowlegde-note"/><summary type="html"><![CDATA[Optuna provodes a complete framwork for Hyperparameter Optimisation.]]></summary></entry></feed>